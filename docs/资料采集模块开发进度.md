# 资料采集模块开发进度

## 2026-05-29 重新梳理

### 模块定位

资料采集模块拆分为两个子模块：

| 子模块 | 核心目标 | 主要产物 |
| --- | --- | --- |
| 知识点采集 | 从教材、考试分析、复习讲义中归纳知识点、考点、历史数据、命题分析 | 知识点 JSON、`knowledge_points`、`topics`、知识点来源记录 |
| 题库采集 | 从 PDF/Word/图片中识别题目、答案、解析、公式和图形 | 题目 JSON、`question_banks`、`questions`、`question_assets` |

两部分最终要打通：

1. 题目关联知识点，写入 `question_knowledge_points`。
2. 历史真题按知识点、年份、题型、分值统计。
3. 统计结果供【统计分析】模块使用，例如知识点热度、分值分布、薄弱点分析。

### JSON 与数据库关系

建议采用“JSON 中间层 + SQLite 权威库”的流程，而不是只选其一。

原因：

- AI 归纳和题目识别天然适合先输出 JSON，便于人工校对、重跑、对比不同模型结果。
- SQLite 更适合作为应用运行时的权威数据源，支持查询、统计、关联、去重和版本管理。
- 对于公式、图形、截图等非文本内容，JSON 只记录结构和引用，真实文件落盘后由数据库记录路径。

推荐流程：

```text
原始资料
  -> 文本/图片/页码抽取
  -> AI 生成结构化 JSON 草稿
  -> 人工校对 JSON
  -> 导入 SQLite
  -> 规则 + AI 辅助关联知识点
  -> 统计分析
```

落地原则：

- JSON 是“草稿、备份、可审计中间产物”。
- SQLite 是“应用读取、写回、统计分析的唯一权威数据源”。
- 数据库写入后仍保留 JSON 导出能力，便于回溯和迁移。

### 当前数据库检查

已查看 `data/knowledge/chunkao.db` 与初始化脚本，当前方向可行。

当前数据库已有：

- `topics`：90 条
- `knowledge_points`：122 条
- `question_banks`：42 条
- `questions`：905 条
- `question_knowledge_points`：561 条
- `question_assets`：764 条

可行点：

- `questions.source_answer` 仍然保留，没有被删除或重命名。
- `my_answer`、`peer_answers`、`ai_answers`、`discussion` 是增量字段，方向上不破坏旧答案链路。
- `question_assets` 已经能承接公式、图形、整页 PDF 页码引用，后续可以扩展为真实截图文件路径。
- `question_knowledge_points` 已具备置信度、来源和备注字段，适合做“规则先关联、AI 低置信度补充、人工确认”的流程。

需要修正/注意：

- `backend/routes/banks.js` 的题库列表已从 SQLite 读取，但单个题库读取 `GET /api/banks/:id` 仍按 JSON 文件名读取。导入脚本生成的数据库 `id` 与 JSON 文件名不完全一致，可能导致列表能看到、点击编辑却找不到文件。
- `DELETE /api/banks/:id` 当前只删除 JSON 文件，没有同步删除 SQLite 中的 `question_banks/questions/question_assets/question_knowledge_points`。
- `POST /api/banks/save` 会保存数据库和 JSON，但没有写入 `paper_type/source_format/source_path`，后续统计来源时需要补齐。
- `source_answer` 写回接口 `POST /api/banks/update-answer` 基本方向正确，但依赖 `bankId + number`。前端若拿到 JSON 的 `paperId` 而不是数据库真实 `bankId`，仍可能写回失败。
- `AIAnswerReference` 先从 JSON 搜索答案库，再从数据库取 `source_answer`。后续应改为优先从 SQLite 读取，JSON 只做备份。

### 知识点采集规划

数据结构建议继续扩展：

| 字段/表 | 用途 |
| --- | --- |
| `knowledge_points.description` | 知识点说明 |
| `knowledge_points.category` | 大类，例如函数、现代文阅读、语法 |
| 新表 `knowledge_sources` 或复用 `source_files` | 记录知识点来自哪份资料、页码、原文片段 |
| 新表 `exam_insights` | 记录命题分析、历史考频、分值趋势 |

AI 助学功能建议输出 JSON：

```json
{
  "subject": "math",
  "version": "2026",
  "sourceFile": "data/docs/math/2026/xxx.pdf",
  "knowledgePoints": [
    {
      "name": "函数单调性",
      "category": "函数",
      "description": "判断函数增减性及区间",
      "examFocus": ["导数判断单调性", "参数取值范围"],
      "history": [
        { "year": 2024, "score": 6, "questionNumber": "17" }
      ],
      "evidence": [
        { "page": 3, "text": "原文片段或摘要" }
      ]
    }
  ]
}
```

### 题库采集规划

题库采集继续沿用现有解析页面，但需要升级为三步：

1. 抽取：PDF/Word 提取文本，公式和图形先保留页码/截图。
2. 校对：人工确认题干、答案、解析、题号、分值、题型。
3. 入库：写入 SQLite，并同步生成 JSON 备份。

公式与图片建议：

- PDF 文本能识别的公式先保存在 `questions.content/source_answer/analysis`。
- 识别不稳定的公式、图形、整题图片，截图为文件，建议目录为 `data/question_assets/{bank_id}/{question_id}/`。
- 数据库 `question_assets` 记录 `asset_type`、`file_path`、`page_number`、`bbox_json`、`description`。
- 前端校对界面显示截图，允许用户补录 LaTeX 或文字说明。

### 下一步任务

P0：

- 修正 `GET /api/banks/:id`，优先从 SQLite 读取题库和题目；找不到时再回退 JSON。
- 修正 `DELETE /api/banks/:id`，数据库和 JSON 备份同步删除。
- 修正 `POST /api/banks/save`，补齐 `source_path/source_format/paper_type`，并保留 `source_answer` 语义。
- 将【知识点学习】中的 AI 参考答案读取与修正链路改为优先使用数据库真实 `bankId`。

P1：

- 新增 `POST /api/docs/save-to-db` 或在 `/api/docs/parse-corrected` 增加 `saveToDb` 参数。
- 新资料采集页拆分为【知识点采集】与【题库采集】两个入口。
- 题库采集页增加知识点候选确认区，写入 `question_knowledge_points`。
- 对数学真题建立截图资产流程，先解决公式和图形丢失问题。

P2：

- 增加知识点管理/合并/重命名页面。
- 增加题目分值、年份、题型、知识点统计接口。
- 让统计分析模块读取 SQLite，不再扫描 JSON 文件。

### 当前结论

数据库改动总体可行，方向也对：保留 `source_answer`，新增答案讨论字段和资产/知识点关联表，能支撑后续核心功能。

但当前仍处于“JSON 与数据库混用”的过渡态，最大风险不是表结构，而是读写入口不统一。下一步应先把题库读取、保存、删除、答案修正统一到 SQLite，再保留 JSON 作为导出备份。这样后续知识点归类、真题统计和图片资产管理才不会互相打架。

## 2026-05-30 题库采集最新进度

### 已完成

- `POST /api/docs/parse-corrected` 的 `saveToDb` 功能已完成，校正后的题库解析结果可直接写入 SQLite `question_banks` 和 `questions`。
- 前端 `DataImport.jsx` 已新增“💾 保存解析结果入库”按钮，支持将当前解析校正结果提交至后端并保存。
- `POST /api/banks/upload-asset` 上传接口已完成，支持将题目截图文件落盘至 `data/question_assets/{bankId}/{questionId}/`，并在 `question_assets` 表中写入元数据。
- 前端题目资源上传已接入，已支持通过前端上传真实截图文件并保存到数据库。
- 已完成真实验证：文件成功写入 `data/question_assets/test_chinese_sample/q1/`，数据库 `question_assets` 表已插入对应记录。

### 验证结果

- `data/question_assets/test_chinese_sample/q1/1780082889533_obfo54.png` 已生成。
- `question_assets` 表中已存在 `test_chinese_sample_q1_1780082889534` 记录。
- `test_chinese_sample_question_bank.json` 已在备份目录生成并包含 `myAnswer/peerAnswers/aiAnswers/discussion` 字段。

### 结论

当前题库采集侧的“解析校正结果入库”和“截图资产落盘”两个关键环节已经完成，可进入下一阶段：
1. 补齐知识点入库与 `question_knowledge_points` 关联流程；
2. 将题库读取与保存逻辑进一步统一到 SQLite；
3. 继续推进知识点草稿校验与入库功能。

## 2026-05-30 知识点采集入库（补充）

### 已完成

- 在前端 `DataImport.jsx` 的知识点采集面板新增按钮“💾 保存知识点到知识库”，可将 AI 生成并人工校对后的 JSON 草稿提交到后端入库。
- 后端新增接口 `POST /api/knowledge/save`，实现了基础的入库逻辑：upsert `topics`、upsert `knowledge_points` 并写入 `topic_knowledge_points`（字段：置信度、来源标注为 `ai-draft`）。
- 对题库采集流程做了体验优化：工具栏的“保存解析结果入库”在页面已有题目时也会启用；`saveCorrectedToDb` 在无校正文本但页面有题目时会回退为直接保存当前题目（复用 `saveQuestionBank`）。
- 国际课程模块修复：选择栏目时如果根目录无 PDF，不再默认设置固定根目录 PDF URL，避免显示 `Cannot GET /api/international/root-file/...` 错误页面。

### 说明与验证建议

- 前端文件：`frontend/src/components/DataImport.jsx`（新增知识点保存按钮、修改保存入库启用逻辑）。
- 后端文件：`backend/routes/knowledge.js`（新增 `POST /api/knowledge/save`）。
- 验证步骤：
  1. 启动后端：`node backend/app.js`。
  2. 启动前端：`cd frontend && npm run dev`。
  3. 在浏览器【资料采集】→切换到【知识点采集】，抽取文本 → AI 生成 JSON → 人工校对 → 点击“💾 保存知识点到知识库”。
  4. 在数据库 `data/knowledge/chunkao.db` 中确认 `topics` 与 `knowledge_points`/`topic_knowledge_points` 已写入对应记录。

### 后续改进（建议）

- 把入库流程扩展为：保存 `source_files`（记录原始资料文件与页码）与 `exam_insights`（命题分析），便于后续统计和回溯。
- 为 `topic_knowledge_points` 增加 `raw_json` 或 `source_fragment` 字段以保留原始证据片段，便于人工复核。
- 当前实现以 `ai-draft` 标注来源，后续应支持人工确认并提升置信度（workflow：draft → reviewed → confirmed）。


## 2026-05-29 P0 修复记录

### 已完成

- `GET /api/banks/:id` 改为优先从 SQLite 读取题库和题目，并转换为前端已有 JSON 字段格式；只有数据库找不到时才回退读取 JSON 文件。
- `GET /api/banks/search` 改为优先在 SQLite 中按规范化标题匹配，JSON 搜索只作为历史备份兜底。
- `DELETE /api/banks/:id` 改为同步删除 SQLite 中的 `question_banks`、`questions`、`question_assets`、`question_knowledge_points`，并清理对应 JSON 备份。
- `POST /api/banks/save` 补齐 `source_path/source_format/paper_type`，保存前先清理同一题库旧题目，避免重复题目残留。
- `POST /api/banks/save` 写入 `my_answer/peer_answers/ai_answers/discussion`，保持答案讨论字段不丢失。
- `GET /api/knowledge/source-answers` 改为用规范化标题在 SQLite 中匹配题库，兼容教师版、学生版、复习讲义、上海专用等后缀差异。
- 前端 `DataImport.jsx` 保存题库时补齐 `myAnswer/peerAnswers/discussion` 字段。

### 验证

- `node --check backend/routes/banks.js` 通过。
- `node --check backend/routes/knowledge.js` 通过。
- `python backend/scripts/test_answer_management.py` 通过。
- `npm.cmd run build` 通过。首次在沙盒内因 `dist` 文件占用报 `EPERM`，提升权限后构建成功。

### 剩余 P0/P1

- 新资料采集页面还没有正式拆成【知识点采集】与【题库采集】两个入口。
- `/api/docs/parse-corrected` 仍只返回解析结果，还没有直接 `saveToDb`。
- 数学公式和图形目前已有 `question_assets` 页码引用，但尚未实现自动截图文件落盘。

## 2026-05-29 P0 续修复：AI 参考答案侧栏

### 已完成

- `frontend/src/components/AIAnswerReference.jsx` 改为通过 `/api/banks/search` 单次加载题库、题目、`sourceAnswer` 和 AI 答案。
- 题号读取改为优先使用数据库返回的 `question.number`，避免题号与数组下标不一致导致写回错位。
- 只有历史 JSON 兜底数据缺少 `sourceAnswer` 时，才额外调用 `/api/knowledge/source-answers`。
- 修正答案仍通过 `POST /api/banks/update-answer` 写回 SQLite 的 `questions.source_answer`。

### 验证

- `node --check backend/routes/banks.js` 通过。
- `node --check backend/routes/knowledge.js` 通过。
- `python backend/scripts/test_answer_management.py` 通过。
- `npm.cmd run build` 通过。沙盒内仍会因 `dist` 文件占用报 `EPERM`，提升权限后构建成功。

### 下一步建议

- 继续拆分【新资料采集】为【知识点采集】和【题库采集】两个入口。
- 优先给题库采集补 `saveToDb` 流程，使 `/api/docs/parse-corrected` 的结果可直接入库。

## 2026-05-29 P1：采集入口拆分

### 已完成

- `frontend/src/components/DataImport.jsx` 增加【知识点采集】与【题库采集】分段入口。
- 【题库采集】保留原有题库解析、AI 验证、答案管理、保存到题库流程。
- 【知识点采集】新增第一版工作台：
  - 选择学科、版本、资料主题、资料文件、页码范围。
  - 调用 `/api/docs/extract-raw` 抽取 PDF/DOCX 文本。
  - 调用 `/api/ai/ask` 生成知识点 JSON 草稿。
  - 支持手动编辑原始文本和 JSON 草稿。
- 知识点采集暂不直接写数据库，符合“先 JSON 草稿、人工校对，再入 SQLite”的路线。

### 验证

- `node --check backend/routes/banks.js` 通过。
- `node --check backend/routes/knowledge.js` 通过。
- `python backend/scripts/test_answer_management.py` 通过。
- `npm.cmd run build` 通过。

### 下一步建议

- 给知识点 JSON 草稿增加“校验 JSON 格式”和“保存草稿文件”功能。
- 新增知识点入库接口，将校对后的 JSON 写入 `knowledge_points`、来源记录和命题分析表。
- 给题库采集补 `saveToDb`，让解析校正后的题目可直接入库。

## 2026-05-29 知识点采集测试修复

### 已修复

- 修复知识点采集中文件选择后，浏览器文件框仍显示“未选择任何文件”的误导问题。
  - 原因：知识点面板作为内部 React 组件渲染，父组件状态更新时会重挂载，导致原生 file input 显示状态被清空。
  - 处理：改为普通渲染函数，并在文件框下方显示“已选择：文件名”。
- 强化知识点 JSON 生成提示词：
  - 新增 `examHistory` 字段，要求完整提取“年份/分值/考点/篇目”命题分析表。
  - 新增 `requiredTexts` 字段，要求完整提取“春考考查篇目”并按册别分组。
  - 明确要求保留高频篇目、易错字词、直接默写、情景默写、文学常识、文化常识等信息。
  - 明确禁止编造，缺失信息使用空数组或 `null`。
- `/api/ai/ask` 支持传入 `numPredict` 和 `temperature`，知识点采集调用时使用 `numPredict: 4096`、`temperature: 0.1`，减少 JSON 信息不完整的问题。

### 说明

后台出现的 PDF warning，如 `Unsupported: field.type of Link`、`TT: undefined function`，来自 PDF 文本抽取库处理字体、链接和表单字段时的提示。只要文本能成功抽取，它们不是 AI 生成 JSON 的直接错误。

### 验证

- `node --check backend/routes/ai.js` 通过。
- `node --check backend/routes/banks.js` 通过。
- `python backend/scripts/test_answer_management.py` 通过。
- `npm.cmd run build` 通过。

## 2026-05-29 知识点层级补强

### 已修复

- 强化知识点采集 JSON schema，`knowledgePoints` 新增 `subPoints` 字段。
- 明确要求“考点一/考点二”作为一级知识点，其下“1./2./3.”编号小点必须逐条保存为 `subPoints`。
- `subPoints` 结构包含：
  - `name`：子考点名称
  - `explanation`：讲解摘要
  - `examples`：典例、题干、答案、分析
  - `commonErrors`：易错字词
  - `highFrequencySentences`：高频默写句
  - `evidence`：原文依据
- 新增自检要求：资料中每个考点下有几个编号小点，JSON 对应 `knowledgePoint.subPoints` 就必须有几个条目。
- 知识点采集调用 `/api/ai/ask` 的 `numPredict` 提高到 `6144`，减少子考点细节被截断。

### 验证

- `node --check backend/routes/ai.js` 通过。
- `python backend/scripts/test_answer_management.py` 通过。
- `npm.cmd run build` 通过。
